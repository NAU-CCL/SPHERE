from abc import ABC, abstractmethod


class Transition(ABC):
    """
    Abstract base class providing an interface for the transition rule of 
    the dynamical system of interest. 
    
    """

    
    
